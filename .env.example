# PostgreSQL Admin (NOT used by the application)
POSTGRES_ADMIN_USER=admin                # Used by the PostgreSQL container to initialize the DB
POSTGRES_ADMIN_PASSWORD=admin123         # Admin password (only used at container bootstrap)
POSTGRES_DB=myappdb                      # Default DB created at startup

# Application user (limited access)
APP_DB_USER=myappuser                    # Non-root DB user created via init.sql
APP_DB_PASSWORD=senha_app_segura         # Password for the limited user
APP_DB_NAME=myappdb                      # Database name the app connects to (same as above)
